<!-- pág onde o usuario pode mudar seus dados, editar seu perfil melhor dizendo -->
<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PublicClass - Tela de Perfil</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <script src="https://unpkg.com/axios/dist/axios.min.js"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="avatar.css">
    <style>
        body {
            margin: 0;
            font-family: 'Arial', sans-serif;
            background-image: url('IMG/fd_mtr.png'), radial-gradient(farthest-corner at center, rgba(36, 123, 160, 1) 0%, rgba(0, 100, 148, 1) 30%, rgba(19, 41, 61, 1) 100%);
            background-repeat: repeat-y;
            background-position: 0% 0%, 100% 100%;
            background-size: cover;
            width: 100%;
            overflow: hidden;
        }

        i {
            border-radius: 25px;
            background: none;
            border: none;
            margin-right: 10px;
        }

        #imgrb {
            width: 200px;
            height: 200px;
            margin-top: -5%;
            margin-right: 10px;

        }

        @font-face {
            font-family: DoubleBass;
            src: url('Fonte/DoubleBass-regular-trial.ttf');
        }

        h1 {
            font-family: DoubleBass;
            margin: 0px;

        }

        /* .nav-buttons {
            position: fixed;
            top: 0;
            left: 0px;
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            background-color: transparent;
            gap: 10px;
        }

        .nav-buttons .btn {
            font-size: 1.2rem;
            color: white;
            font-weight: bold;
            background-color: #ff9600;
            border: none;
            border-radius: 0 50px 50px 0;
            padding: 10px 20px;
            width: 200px;
            text-align: left;
            transition: all 0.3s ease;
            height: 60px;
            display: flex;
            align-items: center;
            gap: 10px;
            margin-left: 0;

        }

        .nav-buttons .btn:hover {
            transform: scale(1.05);
            background-color: #ffd27f;
            color: #1f496e;
        } */

        .nav-buttons {
            position: fixed;
            top: 0;
            left: 0px;
            /* Metade da largura do botão para que só metade apareça */
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            padding: 0;
            background-color: transparent;
            gap: 5px;


        }

        .nav-buttons .btn {
            font-size: 1.2rem;
            color: white;
            font-weight: bold;
            background-color: #ff9600;
            /* Cor laranja dos botões */
            border: none;
            border-top-right-radius: 50px;
            /* Bordas arredondadas */
            border-bottom-right-radius: 50px;
            border-top-left-radius: 0px;
            border-bottom-left-radius: 0px;
            margin-bottom: 10px;
            padding: 10px 20px;
            width: 200px;
            text-align: left;
            transition: all 0.3s ease;
            height: 60px;
            gap: 10px;




        }



        .nav-buttons .btn:hover {
            background-color: #ffd27f;
            /* Laranja claro ao passar o mouse */
            color: #1f496e;
            left: 0;
            transform: scale(1.05);
            /* Ao passar o mouse, o botão aparece por completo */
        }




        /* .nav-buttons .btn:hover {
            transform: scale(1.05);
        } */


        /* Espaço do conteúdo principal */
        .main-content {
            margin-left: 100px;
            /* Espaço para acomodar a parte visível dos botões */
            padding: 30px;
            text-align: right;
            /* Alinha o texto à direita */
        }

        .main-content h1 {
            font-size: 120px;
            font-weight: bold;
            color: #ffffff;
            /* Cor branca para o título */
            margin-bottom: 20px;
            text-align: center;

        }


        .contentPG {
            background-color: #055680;
            border-radius: 25px;
            width: 900px;
            height: 700px;
            margin: 0 auto;
            text-align: center;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .top-buttons {
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
            width: 100%;
            border-bottom: 2px solid white;
        }

        .btn {
            background-color: #F89C1C;
            border: none;
            padding: 10px 20px;
            color: #ffffff;
            font-weight: bold;
            border-radius: 25px;
            cursor: pointer;



        }

        .divider-line {
            width: 2px;
            height: 80px;
            /* Ajusta a altura para alinhar com o título */
            background-color: #ffffff;
            margin: 0 60px;

        }

        .titulo-matematica {

            border-bottom: 1px solid white;
            width: 100%;
            /* Ajusta para alinhar com os botões */
            display: flex;
            align-items: center;
            justify-content: center;
            color: #ffffff;
            font-size: 2rem;
        }

        .tabela-container {
            width: 100%;
            max-height: 100%;
            overflow-y: auto;
            scrollbar-width: none;
            background-color: #055680;
            border-radius: 15px;
        }


        }

        .tabela {
            width: 100%;
            height: 100%;
            color: #ffffff;
        }

        .tabela th,
        .tabela td {
            border: 1px solid #ffffff;
            padding: 10px;
            text-align: center;
            font-size: 16px;
        }

        .ver-btn {
            background-color: #F89C1C;
            border: none;
            padding: 5px 15px;
            color: #ffffff;
            font-weight: bold;

            border-radius: 5px;
        }

        .criar-prova {
            position: fixed;
            top: 86%;
            background-color: #057807;
            border: none;
            padding: 20px;
            color: #ffffff;
            font-size: 18px;
            font-weight: bold;
            margin-top: 20px;
            cursor: pointer;
            border-radius: 25px;
            width: 100%;
            max-width: 200px;
        }



        .user-name {
            display: flex;
            align-items: center;
            border-top-right-radius: 50px;
            /* Bordas arredondadas */
            border-bottom-right-radius: 50px;
            border-top-left-radius: 0px;
            border-bottom-left-radius: 0px;
            background-color: #FF8C00;
            color: white;
            padding: auto;
            border-radius: px;
            width: 300px;
            height: 60px;
            margin-bottom: 10%;
            margin-top: 8%;
        }

        .user-name h2 {
            font-size: 22px;
            padding-top: 10px;
            padding-left: 25px;
            font-family: DoubleBass;

        }

        .perfil_ft {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            padding-left: 10px;



        }


        /* Botões */
        .botao {
            background-color: #FF9800;
            color: #FFFFFF;
            border: none;
            border-radius: 40px;
            padding: 30px 70px;
            margin: 0 60px;
            margin-top: 40px;
            font-size: 2rem;
            font-weight: 600;
            cursor: pointer;
            text-align: center;
        }

        .botao:hover {
            background-color: #FFA726;
        }





        .modal {
            display: none;
            /* Escondido por padrão */
            position: fixed;
            z-index: 1;
            /* Fica em cima de outros elementos */
            left: 0;
            top: 0;
            width: 100%;
            /* Largura total */
            height: 100%;
            /* Altura total */
            overflow: hidden;
            /* Permite rolagem se necessário */
            background-color: rgba(0, 0, 0, 0.5);
            /* Fundo semi-transparente */
        }

        .modal-content {
            background-color: #055680;
            margin: 15% auto;
            /* Centraliza o modal */

            border: 1px solid #888;
            border-radius: 40px;
            width: 60%;
            /* Largura do modal */
            height: 60%;
            overflow-y: auto;
        }

        .close,
        closed {
            position: fixed;
            top: 1%;
            left: 25% color: orange;
            font-size: 200px;
            font-weight: bold;
        }

        .close:hover,
        .close:focus {
            color: black;
            text-decoration: none;
            cursor: pointer;
        }


        .tabela {
            width: 100%;
            border-collapse: collapse;
            color: white;
        }

        .tabela th,
        .tabela td {
            padding: 20px;
            text-align: center;
            border-bottom: 1px solid #ffffff;
        }

        .tabela th.numero,
        .tabela td.numero {
            width: 10%;
            background-color: #055680;
            font-size: 24px;
            font-weight: bold;

        }


        .tabela th.questao,
        .tabela td.questao {
            width: 100%;
            text-align: center;
            font-size: 18px;
            background-color: #055680;
        }

        .questaoC {
            overflow-y: auto;
            max-height: 100px;
        }

        .tabela th.icone,
        .tabela td.icone {
            width: 20%;
        }

        .tabela td.icone img {
            width: 24px;
            height: 24px;
        }

        .modal-content::-webkit-scrollbar,
        .questaoC::-webkit-scrollbar,
        .tabela-container::-webkit-scrollbar {
            display: none;
        }
    </style>
</head>

<body>

    <div class="container-fluid p-0"> <!-- Container principal, usa bootstrap para ocupar toda a largura da página -->
        <div class="row no-gutters"> <!-- Linha sem espaços entre colunas -->

            <!-- Nome do Usuário -->
            <div class="user-name"> <!-- Div que contém o nome e a imagem do usuário -->
                <img class="perfil_ft" src="" alt="" style="margin-left: 15px;">
                <!-- Imagem do perfil, src deve ser preenchido -->
                <h2 id="nomeProfessor">user</h2>
            </div>

            <!-- Botões de Navegação -->
            <div class="col-2 nav-buttons mt-4"> <!-- Coluna para os botões de navegação -->
                <button class="btn" onclick="window.location.href='perfil_prof.html'"><i class="fas fa-user"></i>Meu
                    perfil</button> <!-- Botão para ir ao perfil -->
                <button class="btn" style="width: 250px;" onclick="window.location.href='MontarP1.html'"><i
                        class="fas fa-edit"></i>Montar prova</button> <!-- Botão para montar prova -->
                <button class="btn" onclick="window.location.href='resultados.html'"><i
                        class="fas fa-chart-bar"></i>Resultados</button> <!-- Botão para resultados -->
                <button class="btn" onclick="window.location.href='ajuda.html'"><i
                        class="fas fa-question-circle"></i>Ajuda</button> <!-- Botão para ajuda -->
            </div>

            <!-- Conteúdo Principal -->
            <div class="col main-content"> <!-- Coluna principal onde o conteúdo será exibido -->
                <div class="container mt-5 mb-2">
                    <!-- Container para o conteúdo, com margens superiores e inferiores -->
                    <div class="row"> <!-- Linha para centralizar o título e a imagem -->
                        <div class="col" style="display: flex; align-items: center; justify-content: center;">
                            <!-- Coluna com flexbox para centralização -->
                            <img src="IMG/roboc.png" alt="" id="imgrb"> <!-- Imagem central do robô -->
                            <h1 class="tit text-center">PublicClass</h1> <!-- Título central -->
                        </div>
                    </div>

                    <div class="contentPG">
                        <hr class="divider">
                        <h2 class="titulo-matematica">Suas provas</h2>

                        <div class="tabela-container">

                            <table class="tabela">
                                <thead>
                                    <tr>
                                        <th>Data de criação</th>
                                        <th>Título da Prova</th>
                                        <th>Ações</th>
                                    </tr>
                                <tbody id="tabela-provas">

                                </tbody>
                            </table>
                        </div>

                    </div>

                </div>
            </div>
        </div>
    </div>
    </div>







    <!-- JavaScript e Bootstrap -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>




</body>

<script>
    function getCookie(name) {
        const match = document.cookie.match(new RegExp('(^| )' + name + '=([^;]+)'));
        if (match) {
            return match[2];
        }
        return null;
    }

    // Função para definir um cookie com nome, valor e tempo de expiração
    function setCookie(name, value, days) {
        const date = new Date();
        date.setTime(date.getTime() + (days * 24 * 60 * 60 * 1000)); // Expiração em dias
        const expires = "expires=" + date.toUTCString();
        document.cookie = name + "=" + value + ";" + expires + ";path=/";
    }
</script>

<script>

    axios.get('/provas')
        .then(function (response) {
            const provas = response.data;
            console.log(provas);

            const provasTabela = document.querySelector('#tabela-provas');

            provas.forEach(prova => {
                const linha = document.createElement('tr');
                const colunaData = document.createElement('td');
                const colunaProva = document.createElement('td');
                const colunaAcoes = document.createElement('td');

                const botao = document.createElement('button');

                botao.textContent = 'Iniciar quiz ao vivo';
                botao.classList.add('ver-btn');
                colunaAcoes.appendChild(botao);

                // Função para formatar a data
                const data = new Date(prova.dataCriacao);
                const dia = data.getDate().toString().padStart(2, '0');
                const mes = (data.getMonth() + 1).toString().padStart(2, '0');
                const ano = data.getFullYear();

                const dataFormatada = `${dia}/${mes}/${ano}`;

                colunaData.textContent = dataFormatada;
                colunaProva.textContent = prova.titulo;
                // colunaAcoes.textContent = "Iniciar quiz ao vivo";

                // Configura o conteúdo, o estilo e o evento de clique de colunaAcoes
                colunaAcoes.style.cursor = "pointer";
                colunaAcoes.addEventListener('click', () => {
                    window.location.href = 'professor_questoes.html?provaId=' + prova.id;
                });

                linha.appendChild(colunaData);
                linha.appendChild(colunaProva);
                linha.appendChild(colunaAcoes);
                provasTabela.appendChild(linha);
            });
        })
        .catch(function (error) {
            console.log(error);
        })

    axios.get('/professor')
        .then((response) => {
            const nome = document.querySelector("#nomeProfessor");
            // Campos de texto
            nome.innerHTML = response.data.nome;
            console.log(response.data);
        })
        .catch((e) => {
            console.log(e);
        });
</script>

</html>